# Local development configuration
# Disable Redis and Kafka for local development if they're not needed
REDIS_ENABLED=false
KAFKA_ENABLED=false

# Optional: Configure other settings
LOG_LEVEL=INFO
API_KEY_ENABLED=false
